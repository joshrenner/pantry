<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>{{ title }}</h1>

  	<ul id="pantry">
  		{{#ingredients}}
  			<li><span class="amount">{{quantity.amount}}</span>&nbsp;<span 
					  class="measure">{{quantity.measure}}</span>&nbsp;<span 
					  class="name">{{ingredient.name}}</span></li>
  		{{/ingredients}}
  	</ul>
  </body>
</html>
