<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>{{ title }}</h1>
	<ul>
		{{#ingredients}}
			<li>{{quantity.amount}} {{quantity.measure}} {{ingredient.name}}</li>
		{{/ingredients}}
	</ul>
  </body>
</html>
