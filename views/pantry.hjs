<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>{{ title }}</h1>

  	<ul id="pantry">
  		{{#ingredients}}
  			<li><span class="amount">{{quantity.amount}}</span> <span class="amount">{{quantity.measure}}</span> <span class="amount">{{ingredient.name}}</span></li>
  		{{/ingredients}}
  	</ul>
  </body>
</html>
